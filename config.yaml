gateway:
  host: 0.0.0.0
  port: 3000

services:
  users-service:
    baseUrl: http://users:8080
    routes:
      - path: /users
        method: GET
        auth: required
        roles: [admin, manager]
