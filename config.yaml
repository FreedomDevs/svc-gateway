gateway:
  host: "[::]:9000"
  max_body_size: 10485760

services:
  svc-users:
    baseUrl: http://localhost:9002
    routes:
      - path: /users
        method: GET
        auth: required
        roles: [admin, manager]
